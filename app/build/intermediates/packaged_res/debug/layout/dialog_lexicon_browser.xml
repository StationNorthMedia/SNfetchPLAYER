<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/nord0"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Dialog Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="📖 SN-LEXICON – Artist &amp; YouTube Search"
            android:textColor="@color/nord13"
            android:textSize="15sp"
            android:textStyle="bold" />

        <ImageButton
            android:id="@+id/btnCloseDialog"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Close"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            app:tint="@color/nord4" />

    </LinearLayout>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:text="Select an artist to search YouTube in your browser for playlists &amp; tracks:"
        android:textColor="@color/nord4"
        android:textSize="11sp" />

    <!-- Search Input Bar -->
    <EditText
        android:id="@+id/etLexiconSearch"
        android:layout_width="match_parent"
        android:layout_height="36dp"
        android:layout_marginTop="10dp"
        android:background="@color/nord1"
        android:hint="🔍 Search artist name or bio..."
        android:imeOptions="actionSearch"
        android:inputType="text"
        android:paddingHorizontal="10dp"
        android:textColor="@color/nord6"
        android:textColorHint="@color/nord4"
        android:textSize="12.5sp" />

    <!-- A-Z Index Quick Filter Strip -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:scrollbars="none">

        <LinearLayout
            android:id="@+id/layoutAZButtons"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <!-- Dynamic or predefined A-Z buttons will be populated here -->

        </LinearLayout>
    </HorizontalScrollView>

    <!-- Count Summary -->
    <TextView
        android:id="@+id/tvArtistCount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text="2079 Artists loaded"
        android:textColor="@color/nord8"
        android:textSize="11sp"
        android:textStyle="italic" />

    <!-- Artists List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvLexiconArtists"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="6dp"
        android:layout_weight="1"
        android:scrollbars="vertical" />

</LinearLayout>
